INSTALACIÓN Y CONFIG MANUAL
Se elimina el cdn del archivo index.html

Desde la terminal
-Npm init -y//para generar un package.json que inicializa el proyecto, los paquetes y scripts
-npm install -D tailwindcss// instalación de tailwindcss
FALTA ACTUALIZAR NPM
-crear archivo css desde visual
npx tailwind init// crear archivo de config para los css de tailwind
npx tailwindcss -i tailwind.css -o styles.css // para actualizacion manual
-npx tailwindcss -i tailwind.css -o styles.css –minify// el –minify pone los comandos en una sola línea de código
- npm i -D vite postcss autoprefixer// instalación de vite en el proyecto para que el haga la actualización de los estilos